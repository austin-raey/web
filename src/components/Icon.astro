---
export interface IconProperties {
	name: string;
	title?: string;
}

interface Props extends IconProperties {
	class?: string;
}

const { class: className, name, title } = Astro.props;
const iconSelector = name.startsWith("logos/") ? "i" : "ic";
---

<span
	aria-hidden={title ? undefined : "true"}
	class:list={[iconSelector, name, !title && "pointer-events-none", className]}
	title={title}
>
</span>
