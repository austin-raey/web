---
interface Props {
	class?: string;
	name: string;
	title?: string;
}

const { class: className, name, title } = Astro.props;
---

<span
	aria-hidden={title ? undefined : "true"}
	class:list={["i", name, !title && "pointer-events-none", className]}
	title={title}
>
</span>
